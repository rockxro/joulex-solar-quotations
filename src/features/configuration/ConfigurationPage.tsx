import { PlaceholderPanel } from "@/components/common/PlaceholderPanel";

export function ConfigurationPage() {
  return (
    <PlaceholderPanel
      description="Parámetros editables: tarifas por defecto, factor de pérdidas, DOD, datos de empresa y datos del PDF."
      eyebrow="Gestión"
      title="Configuración"
    />
  );
}
