import { PlaceholderPanel } from "@/components/common/PlaceholderPanel";

export function CatalogPage() {
  return (
    <PlaceholderPanel
      description="CRUD local para productos activos e inactivos del catálogo SQLite."
      eyebrow="Gestión"
      title="Catálogo de productos"
    />
  );
}
